import { ShopAttendancePanel } from "@/components/ShopAttendancePanel";

/** Self-contained attendance / salary page — loads via server actions from the client panel. */
export default function ShopAttendancePage() {
  return <ShopAttendancePanel />;
}
